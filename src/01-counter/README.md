# 🔢 Counter App

**Goal:**  
Build a simple counter application to understand React basics — state management, event handling, and component structure.

---

## 🧠 Concepts Covered
- `useState` hook for managing state
- Handling click events
- Passing props (if using separate Button components)
- Basic JSX structure and styling

---

## ✅ Requirements
1. Display a counter starting at `0`
2. Add three buttons:
   - **Increment (+)** → increases count by 1  
   - **Decrement (-)** → decreases count by 1  
   - **Reset (↻)** → resets count to 0  
3. Prevent counter from going below 0 (optional)
4. (Optional) Add keyboard shortcuts:  
   - `ArrowUp` → increment  
   - `ArrowDown` → decrement  

---

## 💡 Bonus Ideas
- Add a **step selector** (e.g., increment by 5)
- Add upper and lower bounds for the counter
- Persist counter value in `localStorage`
- Add smooth animations using CSS transitions or Framer Motion

---

## 🧩 Example Output

